Syntax error found near Numbered Bind Parameter (WHERE Clause): SELECT TABLE_SCHEMA AS TABLE_CAT, ? AS TABLE_SCHEM, TABLE_NAME, CASE WHEN TABLE_TYPE=? THEN CASE WHEN TABLE_SCHEMA = ? OR TABLE_SCHEMA = ? THEN ? ELSE ? END WHEN TABLE_TYPE=? THEN ? ELSE TABLE_TYPE END AS TABLE_TYPE, TABLE_COMMENT AS REMARKS, ? AS TYPE_CAT, ? AS TYPE_SCHEM, ? AS TYPE_NAME, ? AS SELF_REFERENCING_COL_NAME, ? AS REF_GENERATION FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_SCHEMA = ? AND TABLE_NAME LIKE ? HAVING TABLE_TYPE IN (?,?,?,?,?) ORDER BY TABLE_TYPE, TABLE_SCHEMA, TABLE_NAME
